import 'package:flutter/material.dart';

class BurguerTab extends StatelessWidget {
  const BurguerTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("BurguerTab"));
  }
}
